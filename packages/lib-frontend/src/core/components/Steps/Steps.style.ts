import { StyleSheet } from 'react-native';
import { STEPS_NAVIGATION_HEIGHT } from '@lib/frontend/core/components/Steps/Steps.constants';

export const stepsStyle = StyleSheet.create({
  navigation: {
    height: STEPS_NAVIGATION_HEIGHT,
  },
});

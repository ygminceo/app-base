import { OTP_FIELD_SIZE } from '@lib/frontend/authentication/components/OtpField/OtpField.constants';
import { StyleSheet } from 'react-native';

export const otpFieldStyle = StyleSheet.create({
  style: {
    width: OTP_FIELD_SIZE,
  },
});

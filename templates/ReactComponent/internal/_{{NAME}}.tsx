import React from 'react';
import { _{{NAME}}Props } from '@{{MODULE}}(pathCase)/{{PATH}}(pathCase)/{{NAME}}/internal/_{{NAME}}.model';
import { useStyles } from '@lib/frontend/core/hooks';

export const _{{NAME}} = ({ ...props }: _{{NAME}}Props) => {
  const { styles } = useStyles(props);
  return (
  );
};

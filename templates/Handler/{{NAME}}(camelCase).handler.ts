import { {{NAME}}HandlerModel } from '@lib/backend/{{SERVICE}}(camelCase)/handlers/{{NAME}}(camelCase)/{{NAME}}(camelCase).model';

export const {{NAME}}(camelCase)Handler: {{NAME}}HandlerModel = async ({
  data,
}) => {
  
};
